<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;netstandard2.1;net8.0;net10.0</TargetFrameworks>
    <RootNamespace>FeatherQR.SkiaSharp</RootNamespace>

    <!-- NuGet -->
    <IsPackable>true</IsPackable>
    <!-- Off for the packable projects only: with it on, NuGet promotes every pinned transitive package
         (SkiaSharp native assets, the netstandard Unsafe shim) into this nuspec as a direct dependency,
         and the package would declare a graph it does not have. tools/check_package_deps.cs asserts the
         declared graph after pack. -->
    <CentralPackageTransitivePinningEnabled>false</CentralPackageTransitivePinningEnabled>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <PackageId>FeatherQR.SkiaSharp</PackageId>
    <Description>SkiaSharp rendering and bitmap decoding for FeatherQR: QR Code, Micro QR and rMQR images (PNG, JPEG, WebP, SVG), canvas rendering, custom shapes, gradients and icons. No GDI, no System.Drawing.</Description>

    <!-- API compatibility. No baseline until 2.0.0 ships; set PackageValidationBaselineVersion then. -->
    <EnablePackageValidation>true</EnablePackageValidation>

    <!-- Trimming/NativeAOT, as in FeatherQR.csproj. -->
    <IsAotCompatible Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net7.0'))">true</IsAotCompatible>

    <!-- SBOM -->
    <GenerateSBOM>true</GenerateSBOM>
    <SbomGenerationFetchLicenseInformation>true</SbomGenerationFetchLicenseInformation>
    <SbomGenerationEnablePackageMetadataParsing>true</SbomGenerationEnablePackageMetadataParsing>
    <SbomGenerationManifestInfo>SPDX:2.2</SbomGenerationManifestInfo>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SkiaSharp.QrCode.Tests, PublicKey=" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FeatherQR\FeatherQR.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Sbom.Targets">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="PolySharp">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="SkiaSharp" />
  </ItemGroup>

</Project>
